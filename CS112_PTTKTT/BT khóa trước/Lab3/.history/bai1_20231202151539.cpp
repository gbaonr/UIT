#include <bits/stdc++.h>
using namespace std;

int main()
{
    int m, n;
    cin >> m >> n;

    vector<int> a(n);
    vector<int> sol(n);
    for (int i = 0; i < n; cin >> a[i++]) { };

    sort(a.begin(), a.end());
}