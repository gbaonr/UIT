#include <bits/stdc++.h>
using namespace std;

int main()
{
    int m, n, temp;
    cin >> n >> m;

    vector<pair<int, int>> jobs;
    for (int i = 0; i < n; ++i) {
        cin >> temp;
        jobs.push_back({})
    }
}