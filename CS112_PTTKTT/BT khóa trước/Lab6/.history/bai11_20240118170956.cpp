#include <bits/stdc++.h>
using namespace std;

int n, W;
vector<int> w, p;

int dpFunc(int i)
{
}

int main()
{
    cin >> n >> W;

    w.assign(n, 0);
    p.assign(n, 0);

    for (int i = 0; i < n; ++i) {
        cin >> w[i] >> p[i];
    }
}