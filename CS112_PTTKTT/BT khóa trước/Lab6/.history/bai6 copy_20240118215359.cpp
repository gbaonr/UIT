#include <bits/stdc++.h>
using namespace std;

int n;
vector<tuple<int, int, int>> t;

void dpFunc(int i)
{
}

int main()
{
    cin >> n;

    t.resize(n);

    for (int i = 0; i < n; ++i)
        cin >> a[i] >> b[i] >> p[i];
}