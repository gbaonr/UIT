#include <bits/stdc++.h>
using namespace std;

int main() {
    int v, e, n;
    cin >> n >> e >> n;
    
    int** a = new int*[v + 1];
    for (int i = 1; i <= v; ++i) {
        a[i] = new int[v + 1];
    }
}
