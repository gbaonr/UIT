#include <iostream>
#include <vector>
using namespace std;

float EPS = 1e-6;

void find(int cur, float need, vector<float>& res) {
    for (float i = 0.25; i <= 10 + )
}

int main()
{
    int n;
    cin >> n;

    int need;
    cin >> need;

    vector<int> heso(n);
    for (auto& v : heso)
        cin >> v;

    vector<float> res(n);
    find(0, need, res);
}