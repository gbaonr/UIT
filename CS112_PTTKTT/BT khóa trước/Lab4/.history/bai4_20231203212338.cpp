#include <bits/stdc++.h>
using namespace std;

void find(int startIndex, string s) {
    for (int i = startIndex; i < s.size(); ++i) {
        if (i == s.size() - 1) {

        }
    }
}

int main() {
    string s; cin >> s;
}