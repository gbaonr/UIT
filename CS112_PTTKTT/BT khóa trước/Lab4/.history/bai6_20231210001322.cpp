#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    string s;
    cin >> s;

    map<string, map<string, int>> m;
    for (int i = 0; i < n; i++) {
        string u, v;
        cin >> u >> v;
    }
}