#include <bits/stdc++.h>
using namespace std;

bool find(int cur, long long sum, long long prev, long long prev2, string& s) {
    sum = sum * 10 + s[cur] - '0';

    for (int i = 0; i <= 1; ++i) {
        if (i == 0) {
            // khong chen dau + => tiep tuc chen so vao
        } else {

        }
    }
}

int main() {

}